name: Build WatchOS

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
      - development
      - "*_baseline"

jobs:
  build:
    uses: ./.github/workflows/base_build.yaml
    with:
      destination: 'generic/platform=watchOS'
      scheme: WatchOS
