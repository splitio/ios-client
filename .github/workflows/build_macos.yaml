name: Build MacOS

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
      - development

jobs:
  build:
    uses: ./.github/workflows/base_build.yaml
    with:
      destination: 'platform=macOS,arch=x86_64'
      scheme: Split